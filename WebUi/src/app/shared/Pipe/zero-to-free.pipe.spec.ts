import { ZeroToFreePipe } from './zero-to-free.pipe';

describe('ZeroToFreePipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroToFreePipe();
    expect(pipe).toBeTruthy();
  });
});
